\section{实验}
\label{sec:experiments}

\subsection{实验设置}

\paragraph{数据集。}
我们使用当前 MiniOneRec 复现主线中已经准备好的 Industrial 和 Office Amazon 数据集。Industrial 包含 $36{,}259$ 条训练交互、$4{,}533$ 条测试交互和 $3{,}686$ 个 item；Office 包含 $38{,}924$ 条训练交互、$4{,}866$ 条测试交互和 $3{,}459$ 个 item。两者都沿用仓库中的时间划分。

\paragraph{Tokenizer backbone。}
除非特别说明，本文所有训练时 tokenizer 比较都使用与上游 MiniOneRec 对齐的 \rqvae{} 训练制度：三层量化器、每层 codebook 大小为 $256$、量化 embedding 维度为 $32$、AdamW、学习率 $10^{-3}$、训练 $10{,}000$ epochs、batch size 为 $20{,}480$。

\paragraph{评估指标。}
我们报告三类指标：
\begin{itemize}[leftmargin=1.4em]
  \item \textbf{Probe 指标}：不同 ambiguity bucket 上的 target-better rate，重点关注 same-$l_2$；
  \item \textbf{Tokenizer 指标}：最佳 train-stage collision、最终 \texttt{sid-generate} collision，以及最大冲突大小；
  \item \textbf{局部歧义指标}：平均目标 $l_2$ 叶子数、目标落入多叶 same-$l_2$ bucket 的比例、目标落入深拥挤 same-$l_2$ bucket 的比例，以及在固定前缀下第三级 code 的条件熵。
\end{itemize}

\subsection{设计空间 probe：spectral middle view 的必要性}

我们首先做不改 tokenizer 本体的 graph probe。给定一个现有 top-1 SID 错误样本，我们比较某个 graph view 是否更偏向真实 target，而不是当前被错误偏好的候选。这样可以在接入训练之前，直接检查不同图视图对 local ambiguity 的判别力。

\begin{table}[t]
  \centering
  \caption{same-$l_2$ bucket 上的最佳 middle-view probe 结果。spectral middle view 明显强于第一版 heuristic middle view，这也是 MGR-SID v1 采用 spectral $G_{\mathrm{mid}}$ 的直接原因。}
  \label{tab:probe}
  \begin{tabular}{lccc}
    \toprule
    数据集 & 最佳 heuristic middle view & 最佳 spectral middle view & spectral 视图名称 \\
    \midrule
    Industrial & 0.2747 & 0.5401 & \texttt{fagsp\_mid\_base} \\
    Office & 0.2381 & 0.7619 & \texttt{fagsp\_mid\_base} \\
    \bottomrule
  \end{tabular}
\end{table}

\Cref{tab:probe} 说明，真正值得被显式设计的是 middle-resolution graph structure 本身。对 same-$l_2$ 歧义而言，最佳 spectral middle view 在 Industrial 上几乎把 heuristic middle view 的判别力提升到了两倍，在 Office 上甚至超过三倍。这一结果直接推动我们把 $G_{\mathrm{mid}}$ 从 heuristic proxy 升级为 spectral graph。

\subsection{在 upstream-aligned tokenizer 训练上的正式比较}

接下来我们测试图信号进入 tokenizer 学习后是否仍然成立。这里所有方法都使用 \Cref{tab:provenance} 中已经验证过的 upstream-aligned 训练制度。我们比较 semantic baseline、uniform graph regularization，以及 hierarchy-aware 的 MGR-SID v1。

\begin{table}[t]
  \centering
  \caption{Industrial 上的 tokenizer 比较结果。只看 \texttt{sid-train} 会得出误导性结论；真正的排序应当看最终 \texttt{sid-generate} 后的 SID。}
  \label{tab:main}
  \begin{tabular}{lccc}
    \toprule
    方法 & 最佳 train collision & 最终 index collision & Max conflict \\
    \midrule
    Semantic baseline & 0.1066 & 0.00353 & 2 \\
    Uniform graph regularization & 0.2458 & 0.00597 & 6 \\
    Hierarchy-aware MGR-SID v1 & 0.1319 & \textbf{0.00326} & 2 \\
    \bottomrule
  \end{tabular}
\end{table}

\Cref{tab:main} 给出两个重要事实。第一，uniform graph regularization 明显有害，说明“把 graph 加进去”本身并不够。第二，train-stage collision 与最终 tokenizer 质量并不一致。虽然 semantic baseline 在 raw \texttt{sid-train} collision 上最好，但经过官方 \texttt{sid-generate} 后，hierarchy-aware tokenizer 反而得到最好的最终 SID。这也再次说明，tokenizer 比较必须落在最终导出的 index 上。

\subsection{最终 tokenizer 是否真的降低了 local ambiguity}

仅有更低的 final collision 还不够，我们还需要确认它是否真的改善了我们最关心的失败模式。为此，我们直接比较 baseline final index 与 hierarchy final index 在 same-$l_2$ 层面的结构差异。

\begin{table}[t]
  \centering
  \caption{Industrial 上最终 SID 的局部歧义比较。Hierarchy-aware tokenizer 在 catalog 级和 test-weighted 两种口径下都降低了 same-$l_2$ 的拥挤程度。}
  \label{tab:ambiguity}
  \begin{tabular}{lccc}
    \toprule
    指标 & Baseline & Hierarchy & Delta \\
    \midrule
    Mean target $l_2$ leaf count & 4.7999 & 4.4498 & -0.3501 \\
    Fraction targets in multi-leaf $l_2$ & 0.6894 & 0.6131 & -0.0763 \\
    Fraction targets in $l_2$ with $\ge 4$ leaves & 0.3283 & 0.2828 & -0.0454 \\
    Mean target level-3 entropy under $l_2$ & 1.4533 & 1.2935 & -0.1598 \\
    \bottomrule
  \end{tabular}
\end{table}

\Cref{tab:ambiguity} 表明，这不是简单的 code re-permutation。Hierarchy-aware tokenizer 的确让 same-$l_2$ bucket 更干净：目标 item 平均面对的 $l_2$ 叶子数更少，落在多叶与深拥挤 same-$l_2$ bucket 中的比例更低，同时给定 prefix 后第三级 code 的条件熵也更低。在更细的 movement analysis 中，有 $17.32\%$ 的测试目标从多叶 same-$l_2$ bucket 迁移到单叶 bucket，而只有 $9.68\%$ 迁移到更拥挤的方向。这与我们最初关于 local leaf ambiguity 的诊断完全一致。

\subsection{Tokenizer 收益是否传递到了下游 SFT}

接下来我们把两套最终 Industrial tokenizer 推进到下游 SFT，并比较得到的推荐结果。除了当前 semantic baseline 和 hierarchy-aware tokenizer，我们还加入项目历史 CSV 中最接近当前设定的一条 Industrial SFT 记录，以及历史最强的一条 Industrial SFT 记录，用来判断当前结果在仓库内部的大致位置。

\begin{table}[t]
  \centering
  \caption{Industrial 上的 SFT 结果。Hierarchy-aware tokenizer 提升了短程排序，但还没有形成广义的 $@10+$ 优势，也没有超过历史最强 SFT 设定。}
  \label{tab:sft}
  \begin{tabular}{lcccccc}
    \toprule
    运行 & N@1 & N@3 & N@10 & H@1 & H@3 & H@10 \\
    \midrule
    历史最近设置 & 0.0624 & 0.0798 & 0.0987 & 0.0624 & 0.0929 & 0.1471 \\
    历史最强设置 & \textbf{0.0671} & \textbf{0.0850} & \textbf{0.1037} & \textbf{0.0671} & \textbf{0.0984} & \textbf{0.1509} \\
    当前 baseline & 0.0631 & 0.0777 & 0.0943 & 0.0631 & 0.0882 & 0.1343 \\
    当前 hierarchy & 0.0627 & 0.0802 & 0.0936 & 0.0627 & 0.0929 & 0.1304 \\
    \bottomrule
  \end{tabular}
\end{table}

\Cref{tab:sft} 给出了一个很重要的限定。Hierarchy-aware tokenizer 并没有直接把系统推成新的 end-to-end 最优 SFT。相对于当前 upstream-aligned baseline，它提升了 $\mathrm{NDCG}@3$ 和 $\mathrm{HR}@3$，在 $@5$ 上也有轻微增益，但在 $@10+$ 上仍然落后。这说明 tokenizer 侧收益 \emph{已经} 可以传到下游，只不过目前主要停留在短程排序，而不是全面增益。

\subsection{Evaluate 阶段的错误分布发生了什么变化}

为了理解这种“局部有提升、整体未全面提升”的现象，我们进一步比较两套 SFT 模型在 evaluate 阶段的 SID-aware 诊断结果。这里关注的不是单纯的 aggregate metric，而是错误类型本身是否发生了预期中的变化。

\begin{table}[t]
  \centering
  \caption{Industrial 上的 evaluate 错误分布。Hierarchy-aware tokenizer 明显减少了 same-prefix 错误，并且在拥挤局部目标上更有优势；但与此同时，beam 更少停留在 baseline 的局部语义邻域中。}
  \label{tab:error}
  \begin{tabular}{lccc}
    \toprule
    指标 & Baseline & Hierarchy & Delta \\
    \midrule
    Top-1 error same-$l_1$ rate & 0.1815 & 0.1236 & -0.0580 \\
    Top-1 error same-$l_2$ rate & 0.0525 & 0.0370 & -0.0156 \\
    Beam contains same-$l_1$ & 0.6724 & 0.5248 & -0.1476 \\
    Beam contains same-$l_2$ & 0.3272 & 0.3077 & -0.0194 \\
    Top-1 hit on targets with $l_2 \ge 4$ & 0.0475 & 0.0521 & +0.0047 \\
    Recovery on baseline same-$l_2$ errors & -- & 0.0538 & -- \\
    \bottomrule
  \end{tabular}
\end{table}

\Cref{tab:error} 说明，下游变化是高度结构化的。积极的一面是，hierarchy-aware tokenizer 确实减少了局部 same-prefix 错误：剩余 top-1 miss 中，发生在 same-$l_1$ 或 same-$l_2$ 子树里的比例都明显下降；同时在 $l_2$ fanout 至少为 $4$ 的拥挤目标上，它的 top-1 命中率更高。更重要的是，baseline 的 same-$l_2$ 错误是 hierarchy 最容易恢复的一类错误。这与“hierarchy 正在解决 local leaf ambiguity”这一方法动机完全一致。

但诊断同时揭示了另一面：hierarchy-aware tokenizer 的 beam 更不容易停留在 baseline 的局部语义邻域内。从逐样本比较看，hierarchy 修复了 $63$ 个 baseline 的 top-1 miss，但也打坏了 $65$ 个 baseline 原本正确的 top-1 hit。被修复的样本往往更难、更拥挤：在 baseline 下它们的平均目标 $l_2$ fanout 为 $7.32$，而且 target 往往已经在 top-3 或 top-5 中，只是没有被推到 top-1。相反，被打坏的样本往往是 baseline 本来就已经进入了正确局部子树的例子。这也就解释了为什么 hierarchy 在局部 reranking 和短程指标上有帮助，却还没有形成广义的 $@10+$ 增益。

\subsection{当前结果的解释}

综合来看，当前实验支持一个谨慎但连贯的结论。我们 \emph{还没有} 证明已经得到新的 end-to-end 最优系统，也还没有涉及 RL，但我们已经证明：
\begin{enumerate}[leftmargin=1.4em]
  \item 公平的 tokenizer 比较必须使用上游 MiniOneRec 的训练制度；
  \item graph-structured collaborative information 对 local same-$l_2$ disambiguation 确实有信号；
  \item 其中最关键的图视图是 spectral middle-resolution graph；
  \item 将这些图结构以 hierarchy-aware regularization 的方式写回 semantic tokenizer 之后，最终生成 SID 质量优于公平 semantic baseline，并且 same-$l_2$ 局部歧义显著降低；
  \item 这些 tokenizer 收益能够以“局部、结构化”的形式传到下游 SFT，但当前 tokenizer-to-SFT 的接口仍然会牺牲一部分 baseline 已经正确的局部 routing。
\end{enumerate}

对当前项目阶段而言，这比简单宣称“下游赢了”更有价值。因为我们现在已经知道：方法真正带来的，是对 crowded local ambiguity 的修复；而下游没能全面变强的关键，不是方向错了，而是这些结构收益还没有被 SFT 充分接住。
