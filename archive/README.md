# Archive

This directory stores non-mainline code and historical documents that are intentionally excluded
from the active execution chain.

Current archive areas:

- `gpr/`: GPR-related variants kept only for reference
- `old_docs/`: historical notes that are not part of the active mainline documentation

Code under `archive/` must not be imported by the refactored mainline.

