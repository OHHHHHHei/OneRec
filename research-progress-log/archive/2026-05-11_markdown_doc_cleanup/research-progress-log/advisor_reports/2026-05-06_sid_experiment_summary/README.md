# OneRec SID Advisor Report（导师汇报）

Main LaTeX source（主 LaTeX 源文件）:

- `main.tex`

Recommended compile command（推荐编译命令）:

```bash
xelatex main.tex
```

or（或者）:

```bash
lualatex main.tex
```

Local compile note（本机编译说明）:

- This machine currently has a broken/mixed TeX Live setup（TeX Live 版本混用）.
- The root filesystem（根分区） is also full, so the report is stored under `/data/leejt/OneRec/advisor_reports/`.
- The source file itself is ready; compile it on a machine with a normal Chinese LaTeX environment（中文 LaTeX 环境）, e.g. TeX Live 2023+ / Overleaf.

Verified sources（已核实来源） are listed at the end of `main.tex`.
