# Tokenizer Config（分词器配置）

Status（状态）: `pointer（指针）`

The current mainline tokenizer（当前主线分词器） is:

- `r690b_lmh_l2_contrastive_pull_weight001`

Its generated index（生成索引） is:

- `/data/leejt/OneRec/output_weights/experiments/mgr_sid_l2_lmh_sweep_20260507/generated_indices/Industrial_and_Scientific.r690b_lmh_l2_contrastive_pull_weight001.index.json`

The exact training run config（训练配置） was created by the L2 sweep launcher（第二层扫描启动器） during the stage-1 sweep. The stable method settings（稳定方法设置） are recorded in:

- [../../r690b_lmh_l2_contrastive_pull_weight001_generate_summary.json](/home/leejt/OneRec/idea-discovery/2026-05-06_l2_local_multihop_rescue_tokenizers/mainline/r690b_lmh_l2_contrastive_pull_weight001_generate_summary.json)
- [../../r690b_lmh_l2_contrastive_pull_weight001_structure_summary.json](/home/leejt/OneRec/idea-discovery/2026-05-06_l2_local_multihop_rescue_tokenizers/mainline/r690b_lmh_l2_contrastive_pull_weight001_structure_summary.json)

For new tokenizer experiments（新分词器实验）, use candidate-specific config directories（候选配置目录） under `active_candidates/` or `ablations/`.
