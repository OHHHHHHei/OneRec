# L3 Ranking Candidate（第三层排序损失候选）

Status（状态）: `running（运行中）`

Current tmux session（当前会话）:

- `mgr_l3_rank_sft_0509`

Role（角色）:

- Test replacing L3 local pull（第三层局部拉近） with ranking loss（排序损失） while keeping the L2=0.010 anchor（第二层 0.010 锚点）.

Current caution（当前注意）:

- This job is still running.
- Original config/script paths（原始配置/脚本路径） should not be moved until it finishes.
