# Active Candidates（活跃候选）

Status（状态）: `navigation（导航）`

This directory contains near-term candidates（近期候选） that are still useful for deciding the next mainline step（主线下一步）.

Candidate groups（候选组）:

- `l2_square/`: L2 square local graph（第二层平方局部图）
- `l3_ranking/`: L3 ranking loss（第三层排序损失）
- `l1_sweep/`: L1 semantic weight tuning（第一层语义权重调节）
- `office_transfer/`: Office dataset transfer（Office 数据集迁移）
- `recipe_ablation/`: downstream recipe ablation（下游配方消融）

Rule（规则）:

- Candidate configs/scripts（候选配置/脚本） are linked here for navigation.
- Original paths（原始路径） remain the execution source（执行源） until all running jobs finish.
