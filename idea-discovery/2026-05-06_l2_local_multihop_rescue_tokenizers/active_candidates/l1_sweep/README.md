# L1 Sweep Candidate（第一层权重候选）

Status（状态）: `active-candidate（活跃候选）`

This candidate tests whether L1 semantic pull strength（第一层语义拉近强度） can improve the balance between semantic routing（语义路由） and downstream learnability（下游可学习性）.

Current representative（当前代表）:

- `r690b_lmh_l1_weight040_l2_weight001_l3_weight002`

Linked configs and scripts（链接配置和脚本） are in this directory.
