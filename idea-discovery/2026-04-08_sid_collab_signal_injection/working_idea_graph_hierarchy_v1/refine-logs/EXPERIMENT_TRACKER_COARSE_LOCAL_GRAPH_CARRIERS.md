# Experiment Tracker（实验跟踪表）

| Run ID | Milestone（里程碑） | Purpose（目的） | System / Variant（系统 / 变体） | Split（划分） | Metrics（指标） | Priority（优先级） | Status（状态） | Notes（备注） |
|---|---|---|---|---|---|---|---|---|
| D530 | M0 | graph diagnostics（图诊断） | multi-hop `G_local` topology summary | Industrial | coverage, degree, components | MUST | COMPLETED | candidate changed neighborhood size a lot, but did not meaningfully raise item coverage over baseline `local_purified` |
| D540 | M0 | graph diagnostics（图诊断） | user-segment `G_coarse` summary | Industrial | segment diversity, overlap, degree | MUST | COMPLETED | current `K=4,8` formulations stayed too close to baseline coarse graph |
| D541 | M0 | graph diagnostics（图诊断） | `CIR`-reweighted `G_coarse` summary | Industrial | CIR distribution, overlap, degree | MUST | COMPLETED | useful low-risk audit, but current blended reweighting is still near-baseline |
| D542 | M0 | graph diagnostics（图诊断） | `MGDCF` coarse reconstruction summary | Industrial | connected rate, overlap, top-k expansion | MUST | COMPLETED | first coarse candidate that truly reconstructs the mother graph; `r=0.20` is the best first promotion point |
| R541 | M1 | tokenizer screen（分词器筛选） | `L1 <- coarse_CIR`, `L2/L3` fixed | Industrial | generated collision, prefix balance | MUST | ON_HOLD | current first-pass `CIR` formulation stayed too close to baseline; re-launch only after a stronger coarse definition |
| R530a | M2 | tokenizer screen（分词器筛选） | `L3 <- A + αA^2` | Industrial | generated collision, local coverage, same-`l2` | MUST | COMPLETED_NEGATIVE | generated collision degraded sharply to `107 / 3686 = 0.02903`; local multi-hop is not a viable promoted tokenizer candidate in its current form |
| R530b | M2 | tokenizer screen（分词器筛选） | `L3 <- A + αA^2 + α^2A^3` | Industrial | generated collision, local coverage, same-`l2` | NICE | ON_HOLD | do not continue deeper local expansion unless coarse reconstruction later reopens this branch |
| R542a | M2 | tokenizer screen（分词器筛选） | `L1 <- coarse_mgdcf`, `L2 <- fagsp_mid_mgdcf`, `L3 <- local_purified` | Industrial | generated collision, prefix balance, same-`l2` | MUST | COMPLETED_NEGATIVE | first true coarse-reconstruction tokenizer candidate; `42 / 3686 = 0.01139` is far better than `R530a` but still clearly weaker than current `v2` and stage-3 candidates |
| R542b | M2 | tokenizer screen（分词器筛选） | `L1 <- coarse_mgdcf(r=0.20)`, `L2 <- fagsp_mid_base`, `L3 <- local_purified` | Industrial | generated collision, prefix balance, same-`l2` | MUST | IN_PROGRESS | coarse-only isolation for the stronger `r=0.20` MGDCF candidate; tests whether `fagsp_mid_mgdcf` was the main source of regression |
| R542c | M2 | tokenizer screen（分词器筛选） | `L1 <- coarse_mgdcf(r=0.10)`, `L2 <- fagsp_mid_base`, `L3 <- local_purified` | Industrial | generated collision, prefix balance, same-`l2` | MUST | IN_PROGRESS | coarse-only isolation for the lighter `r=0.10` MGDCF candidate; parallel sensitivity check because GPUs are idle |
| R540a | M3 | tokenizer screen（分词器筛选） | user-segment `G_coarse` main variant | Industrial | generated collision, prefix balance, same-`l2` | MUST | ON_HOLD | current offline graph is too close to baseline; likely needs a stronger formulation first |
| R540b | M3 | tokenizer screen（分词器筛选） | stronger segment weighting | Industrial | generated collision, prefix balance, same-`l2` | NICE | TODO | only if `R540a` is stable |
| R550 | M4 | downstream verdict（下游裁决） | best of `R542* / R540* / R541` -> `title_history2sid_on + desc_align_p05` | Industrial | `NDCG@10`, `HR@10`, beam pattern | MUST | TODO | promote one candidate only after the current `R542b/R542c` isolation verdict |
| R560 | M5 | optional combo（可选组合） | best coarse + best local | Industrial | generated collision, downstream if justified | NICE | TODO | run only after a clear single-branch positive |
